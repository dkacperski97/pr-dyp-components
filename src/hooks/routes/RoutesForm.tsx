import React from "react";
import { OptionsProps } from "../../types/props";

const RoutesForm: React.FC<OptionsProps> = ({ site, setSite, componentId, variableId }) => {
    return null;
};

export default RoutesForm;
