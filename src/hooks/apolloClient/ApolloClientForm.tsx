import React from "react";
import { OptionsProps } from "../../types/props";

const ApolloClientForm: React.FC<OptionsProps> = ({ site, setSite, componentId, variableId }) => {
    return null;
};

export default ApolloClientForm;
