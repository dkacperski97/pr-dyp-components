import React from 'react';
import Component from '../types/component';

const list: Component = {
    id: 'list',
    options: [],
    component: () => {
        return (
            <div></div>
        );
    },
    template: ''
}

export default list;
